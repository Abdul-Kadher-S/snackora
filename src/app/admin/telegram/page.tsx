import React from 'react';
import { TelegramClient } from './TelegramClient';

export const metadata = {
  title: 'Telegram Alerts | Snackora Admin',
};

export default function TelegramPage() {
  return <TelegramClient />;
}
